import { HiLoStringPipe } from './hi-lo-string.pipe';

describe('HiLoStringPipe', () => {
  it('create an instance', () => {
    const pipe = new HiLoStringPipe();
    expect(pipe).toBeTruthy();
  });
});
